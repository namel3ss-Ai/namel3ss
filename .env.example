# Optional: change the host port mapped to Studio.
N3_PORT=8080

# Required only if you enable the Postgres service in docker-compose.yml.
POSTGRES_PASSWORD=changeme

# Optional: only needed for OpenAI-backed capabilities.
OPENAI_API_KEY=

# Linux only: match container user to host user to avoid volume permission issues
UID=1000
GID=1000

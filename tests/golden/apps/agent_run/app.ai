spec is "1.0"

ai "assistant":
  provider is "mock"
  model is "gpt-4.1"
  system_prompt is "You are a helper."

agent "planner":
  ai is "assistant"
  system_prompt is "Plan."

flow "demo":
  run agent "planner" with input: "Task" as plan
  return plan

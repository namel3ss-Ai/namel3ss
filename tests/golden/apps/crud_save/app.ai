spec is "1.0"

record "Note":
  field "id" is number
  field "title" is text must be present
  field "body" is text

flow "demo":
  set state.note with:
    title is "First"
    body is "Body"
  save "Note"

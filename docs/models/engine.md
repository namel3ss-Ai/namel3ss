# Engine

The engine runs your flows, pages, and tools. It uses the app, config, and proof to decide what is allowed.

**Example**
```ai
flow "health":
  return "ok"
```

**Command**
- `n3 explain --why`

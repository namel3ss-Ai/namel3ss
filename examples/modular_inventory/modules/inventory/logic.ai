spec is "1.0"

record "Item":
  name text must be present
  price number must be greater than 0

flow "seed_item":
  set state.item.name is "Sample"
  set state.item.price is 12.5
  create "Item" with state.item as item
  return "seeded"

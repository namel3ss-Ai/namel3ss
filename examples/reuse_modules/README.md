# Reuse Modules Example

This example shows module files, merge order, and overrides.
app.ai imports two modules and allows override.
conflict.ai shows a conflict with no override.

Run
n3 app.ai run
n3 app.ai studio

Traces include module_loaded, module_merged, and module_overrides.

{
  "what": "Tool \"get json from web\" cannot use the network.",
  "why": "Effective guarantees forbid network access (GET https://example.com/data).",
  "fix": "Remove the restriction or choose a tool that does not need the capability.",
  "example": "[capability_overrides]\\n\"get json from web\" = { no_network = true }"
}

# Reference app: release_hub

This app is read-only and exists to validate release readiness in CI.

What it covers:
- UI system and manifest generation
- Story steps with next links
- Flows and UI actions
- Agents and team declarations
- Media + icon intent
- Access control via requires rules
